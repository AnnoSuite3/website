{
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "linaria": "^1.3.1",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
